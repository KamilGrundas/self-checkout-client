FROM rust:1.97.1-bookworm@sha256:77fac8b98f9f46062bb680b6d25d5bcaabfc400143952ebc572e924bcbedc3fa

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        clang \
        cmake \
        libasound2-dev \
        libgl1-mesa-dev \
        libudev-dev \
        libv4l-dev \
        libwayland-dev \
        libxkbcommon-dev \
        pkg-config \
    && rm -rf /var/lib/apt/lists/*

RUN rustup component add rustfmt clippy

WORKDIR /workspace

CMD ["bash", "-c", "cargo fmt --check && cargo check --locked && cargo test --locked && cargo clippy --locked --all-targets --all-features -- -D warnings"]
